# iStore 上线后功能更新清单

> 时间：2026-08-06（上线后的首次功能迭代）
> 共 5 大项 23 个改动，全部编译验证通过

---

## 一、新增功能（6 项）

### 1. 智能通知
- **断网告警**：检测到路由器离线时推送系统通知，恢复联网自动通知
- **新设备提醒**：发现新接入设备（MAC 对比）时提醒
- 通知设置页：总开关 / 断网告警 / 新设备提醒独立配置 + 通知权限申请

### 2. 定时任务
- **定时重启**：选择时间 + 星期，到点自动重启路由器
- **定时 WiFi**：定时关闭/开启无线网络（如夜间关 WiFi）
- 说明：受路由器权限限制，任务由 App 本地检测执行（需 App 在运行）

### 3. DHCP 静态绑定（固定 IP）
- 设备详情页「设备管理」新增"📌 固定 IP"：输入 IP 一键绑定
- 绑定后设备重启地址不变（NAS/摄像头刚需）；支持一键解除

### 4. WiFi 访客网络
- WiFi 管理页新增"🛜 访客网络"：独立访客 SSID + 密码
- 三重隔离：独立网段 + 无线客户端隔离 + 防火墙仅允许访问外网
- **访客能上网但无法访问内网设备**，一键开启/关闭

### 5. 流量统计（周报/月报）
- 首页自动记录每日下载/上传流量（本地存储，保留 60 天）
- 应用中心 → 流量统计：近 7/30 天柱状图 + 汇总（总下载/总上传/日均）+ 每日明细

### 6. Docker 增强
- **镜像列表视图**：镜像名 / ID / 大小 / 创建日期（悬空镜像智能标注）
- **网络列表视图**：网络名 / ID / 驱动类型 / 作用域
- 容器/镜像/网络三视图胶囊按钮切换

---

## 二、交互与体验优化（7 项）

### 1. 相册查看器导航条动画修复
- 打开/关闭照片时，主页悬浮导航条不再"从顶部滑下"，改为原位瞬间隐藏/出现

### 2. 查看器手势穿透修复
- 全屏查看照片时左右滑动不再误切到其他 Tab（强制停留在相册）

### 3. 系统按钮跳转修复
- 点"系统"进应用中心不再乱跳首页；**返回时恢复到进入前的 Tab**（设备→系统→返回→设备）

### 4. 视频全屏自动横屏
- 播放器改为**自定义控制条**：播放/暂停 + 进度拖动 + 时长 + 全屏按钮
- **点全屏图标直接横屏铺满**（无需旋转手机），再点恢复竖屏；点击画面隐藏控制条

### 5. 沉浸光感
- 状态栏/导航栏**完全透明沉浸**（图标颜色随深浅色主题自动切换）
- 卡片增加柔和投影光感（深浅主题差异化）

### 6. 版本显示
- 登录页本机信息卡片右上角显示软件版本（如 v2.8.5）

### 7. 设备添加/编辑改弹窗
- "+ 添加新设备"移到列表下方居中，点击弹出半屏表单
- 编辑设备同样弹窗（自动带出已存信息）

---

## 三、视觉与图标（3 项）

### 1. 相册视频图标鸿蒙原生
- 视频缩略图角标：🎬 → `play_video_fill` 原生图标
- 播放按钮：▶ → `play_circle_fill` 原生圆形图标

### 2. Docker 容器图标修复
- 远程 logo 加载失败不再显示空白，自动回退 icon_docker.svg

### 3. Docker 悬空镜像智能显示
- 无标签镜像（`<none>:<none>`）改为显示短 ID + "悬空镜像（无标签）"橙色标注

---

## 四、登录页重构（3 项）

### 1. 移除导入设备功能
- 删除"导入设备"按钮及剪贴板解析逻辑（此前多次尝试修复剪贴板读取均不理想）

### 2. 移除设备复制功能
- 删除设备列表的"📋 复制"按钮（与导入配套下线）

### 3. 版本号统一管理
- 版本真源收敛到 `oh-package.json5` 的 version 字段，运行时统一读取
- 移除全部硬编码版本号（AboutPage / AppsPage / LoginPage 自动同步）

---

## 五、Bug 修复（4 项）

### 1. 通知/定时任务页状态栏避让
- 返回/保存按钮被状态栏遮挡 → 按状态栏高度自适应避让

### 2. 剪贴板读取 API 兼容
- 导入相关：API 24 的同步读取在 API 23 设备不可用 → 改用兼容版本（随导入功能一并下线）

### 3. 沉浸窗口适配
- 全屏布局下状态栏图标颜色跟随主题（深色白字 / 浅色黑字）

### 4. Docker 镜像加载
- 远程 logo 404 死代码修复（见视觉项 2）

---

## 涉及的主要文件

| 模块 | 文件 |
|---|---|
| 智能通知/定时任务 | NotifyStore / ScheduleStore / NotificationWatcher / NotifySettingsPage / SchedulePage |
| DHCP 静态绑定 | ApiClient（uci dhcp host）/ ClientDetailPage |
| 访客网络 | ApiClient（network/dhcp/wireless/firewall 四配置）/ WifiPage |
| 流量统计 | TrafficStatStore / TrafficReportPage / DashboardTab |
| Docker | ApiClient（/images /networks）/ DockerTab / Models |
| 视频播放器 | AlbumTab（自定义控制条） |
| 沉浸光感 | StoreAbility / WindowUtils / ThemeColors |
| 登录页 | LoginPage（bindSheet 弹窗）/ AppVersion / WindowUtils |
| 导航交互 | MainPage（Tab 拦截/返回恢复/动画） |
